# Launch Pro Desk Shell and ensure the state mailbox exists.
exec-once = mkdir -p ~/.local/state/pro-desk-shell
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = pidof pro-desk-shell || pro-desk-shell &
